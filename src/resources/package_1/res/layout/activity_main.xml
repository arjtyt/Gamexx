<?xml version='1.0' encoding='utf-8' ?>
<FrameLayout android:background="@color/splash_screen_background"
             android:layout_width="fill_parent"
             android:layout_height="fill_parent" xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto">
  <RelativeLayout android:id="@id/splash_screen_relative_layout"
                  android:background="@color/splash_screen_background"
                  android:visibility="visible"
                  android:layout_width="fill_parent"
                  android:layout_height="fill_parent">
    <ImageView android:id="@id/splash_image"
               android:background="@mipmap/ic_launcher"
               android:layout_width="150.0dp"
               android:layout_height="150.0dp"
               android:layout_marginTop="300.0dp"
               android:layout_centerHorizontal="true" />
    <TextView android:textSize="30.0sp"
              android:textStyle="bold"
              android:textColor="@color/white"
              android:gravity="center"
              android:layout_gravity="center"
              android:id="@id/app_name"
              android:paddingTop="15.0dp"
              android:visibility="visible"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:text="@string/app_name"
              android:layout_below="@id/splash_image"
              android:layout_centerInParent="true" />
    <TextView android:textSize="12.0sp"
              android:textStyle="normal"
              android:textColor="@color/white"
              android:gravity="center"
              android:layout_gravity="center"
              android:id="@id/splash_subtitle"
              android:visibility="visible"
              android:layout_width="fill_parent"
              android:layout_height="wrap_content"
              android:text="@string/splash_subtitle"
              android:layout_below="@id/app_name"
              android:fontFamily="sans-serif-light" />
  </RelativeLayout>
  <RelativeLayout android:id="@id/web_view_relative_layout"
                  android:visibility="gone"
                  android:layout_width="fill_parent"
                  android:layout_height="fill_parent">
    <FrameLayout android:id="@id/frameLayoutHorizontalProgress"
                 android:background="@color/splash_screen_background"
                 android:layout_width="fill_parent"
                 android:layout_height="4.0dp">
      <ProgressBar android:id="@id/progressbar"
                   android:layout_width="fill_parent"
                   android:layout_height="4.0dp"
                   style="?android:attr/progressBarStyleHorizontal" />
    </FrameLayout>
    <WebView android:id="@id/web_view"
             android:background="@color/splash_screen_background"
             android:layout_width="fill_parent"
             android:layout_height="fill_parent"
             android:layout_below="@id/frameLayoutHorizontalProgress" />
  </RelativeLayout>
</FrameLayout>